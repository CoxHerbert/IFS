# 运维文档索引

本目录存放部署、初始化、升级和维护相关文档。

## SQL 与数据库

- [SQL 初始化与升级顺序](sql.md)
  包含当前保留脚本、全新环境执行顺序、增量升级建议，以及消息通知相关脚本说明。

## 使用建议

1. 全新环境先按 `sql.md` 的顺序执行初始化脚本。
2. 已有环境不要直接重跑带 `DROP TABLE` 的全量脚本，应按现场差异编写增量 SQL。
3. 新增业务表、菜单、权限后，同步在 `sql.md` 中补充脚本用途和执行建议。
4. Swagger 文件属于生成产物，接口变化后重新生成，不在运维文档中手工维护。
